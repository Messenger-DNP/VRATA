package ru.vrata.backend.domain.service;

import org.springframework.stereotype.Service;

@Service
public class MessageService {
    // TODO (producer, logic): check that user is in this room, create message,
    //  send message using Kafka-related class (will be in infrastructure package)
}
